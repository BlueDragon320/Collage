#include <stdio.h>
int main(){
    int l,b,area;
    printf("Enter the values of length and breadth\n");
    scanf("%d %d", &l ,&b);
    area=l*b;
    printf("Area of rectangle=%d\n10", area);
    return 0;
}